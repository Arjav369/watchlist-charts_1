# Watchlist Charts

A self-hosted multi-stock chart grid for NSE momentum screening.
Add symbols, view daily/weekly/monthly TradingView charts side by side.

## Local development

```bash
npm install
npm run dev
```

## Deploy

Push this repo to GitHub, then import it on vercel.com — no build
configuration needed, Vercel auto-detects the Vite + React setup.
